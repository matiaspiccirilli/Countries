
const initialState = {
    countries: [],
}

const action = {}

const rootReducer = (state = initialState) => {
    switch(action.type) {
        default:
            return {...state}
    }
}

export default rootReducer